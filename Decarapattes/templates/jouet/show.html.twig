{% extends 'base.html.twig' %}

{% block title %}Jouet{% endblock %}

{% block body %}
    <h1>Jouet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jouet.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ jouet.reference }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ jouet.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ jouet.prix }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ jouet.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jouet_index') }}">back to list</a>

    <a href="{{ path('jouet_edit', {'id': jouet.id}) }}">edit</a>

    {{ include('jouet/_delete_form.html.twig') }}
{% endblock %}
