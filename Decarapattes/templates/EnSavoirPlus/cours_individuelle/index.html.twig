{% extends 'base.html.twig' %}

{% block title %}CoursIndividuelle index{% endblock %}

{% block body %}
    <h1>CoursIndividuelle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cours_individuelle in cours_individuelles %}
            <tr>
                <td>{{ cours_individuelle.id }}</td>
                <td>
                    <a href="{{ path('cours_individuelle_show', {'id': cours_individuelle.id}) }}">show</a>
                    <a href="{{ path('cours_individuelle_edit', {'id': cours_individuelle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cours_individuelle_new') }}">Create new</a>
{% endblock %}
