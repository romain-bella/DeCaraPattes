{% extends 'base.html.twig' %}

{% block title %}RehabilitationAgressif index{% endblock %}

{% block body %}
    <h1>RehabilitationAgressif index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rehabilitation_agressif in rehabilitation_agressifs %}
            <tr>
                <td>{{ rehabilitation_agressif.id }}</td>
                <td>
                    <a href="{{ path('rehabilitation_agressif_show', {'id': rehabilitation_agressif.id}) }}">show</a>
                    <a href="{{ path('rehabilitation_agressif_edit', {'id': rehabilitation_agressif.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('rehabilitation_agressif_new') }}">Create new</a>
{% endblock %}
