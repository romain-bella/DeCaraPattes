{% extends 'base.html.twig' %}

{% block title %}Croquette{% endblock %}

{% block body %}
    <h1>Croquette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ croquette.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ croquette.reference }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ croquette.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ croquette.prix }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ croquette.quantite }}</td>
            </tr>
            <tr>
                <th>GoutCroquette</th>
                <td>{{ croquette.goutCroquette }}</td>
            </tr>
            <tr>
                <th>TailleCroquette</th>
                <td>{{ croquette.tailleCroquette }}</td>
            </tr>
            <tr>
                <th>TypeCroquette</th>
                <td>{{ croquette.typeCroquette }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('croquette_index') }}">back to list</a>

    <a href="{{ path('croquette_edit', {'id': croquette.id}) }}">edit</a>

    {{ include('croquette/_delete_form.html.twig') }}
{% endblock %}
