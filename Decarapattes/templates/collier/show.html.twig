{% extends 'base.html.twig' %}

{% block title %}Collier{% endblock %}

{% block body %}
    <h1>Collier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collier.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ collier.reference }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ collier.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ collier.prix }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ collier.quantite }}</td>
            </tr>
            <tr>
                <th>TailleCollier</th>
                <td>{{ collier.tailleCollier }}</td>
            </tr>
            <tr>
                <th>CouleurCollier</th>
                <td bgcolor="{{ collier.couleurCollier }}"> </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collier_index') }}">back to list</a>

    <a href="{{ path('collier_edit', {'id': collier.id}) }}">edit</a>

    {{ include('collier/_delete_form.html.twig') }}
{% endblock %}
