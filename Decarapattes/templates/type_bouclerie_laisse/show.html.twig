{% extends 'base.html.twig' %}

{% block title %}TypeBouclerieLaisse{% endblock %}

{% block body %}
    <h1>TypeBouclerieLaisse</h1>

    <table class="table table-dark table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_bouclerie_laisse.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ type_bouclerie_laisse.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_bouclerie_laisse_index') }}">back to list</a>

    <a href="{{ path('type_bouclerie_laisse_edit', {'id': type_bouclerie_laisse.id}) }}">edit</a>

    {{ include('type_bouclerie_laisse/_delete_form.html.twig') }}
{% endblock %}
