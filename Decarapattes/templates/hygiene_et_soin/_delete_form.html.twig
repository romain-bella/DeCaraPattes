<form method="post" action="{{ path('hygiene_et_soin_delete', {'id': hygiene_et_soin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hygiene_et_soin.id) }}">
    <button class="btn">Delete</button>
</form>
